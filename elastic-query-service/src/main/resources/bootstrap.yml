spring:
  application:
    name: elastic-query-service
  profiles:
    active: elastic_query
  cloud:
    config:
      name: elastic-query-service,config-client
      uri: http://localhost:8888
      username: spring-config-user
      password: springConfigPass?